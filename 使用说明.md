# LinkerHand HTTP API 服务 - 打包和使用说明

本指南说明如何将 LinkerHand HTTP API 服务打包成可执行文件，以及如何部署和使用。

## 前置要求

1. Python 3.7+ 已安装
2. 所有依赖已安装：`pip install -r requirements.txt`
3. Windows 操作系统

## 打包服务

### 方法一：使用打包脚本（推荐）

运行打包脚本：
```bash
python build.py
```

或双击运行：
```bash
quick_build.bat
```

打包完成后，`dist` 文件夹将包含：
- `linkerhand_service.exe` - 主程序
- `deploy.bat` - 一键部署脚本（配置开机自启动）
- `start_service.bat` - 启动服务
- `stop_service.bat` - 停止服务
- `restart_service.bat` - 重启服务
- `uninstall_service.bat` - 卸载服务（删除开机自启动）
- `README.txt` - 使用说明

### 方法二：手动打包

使用 PyInstaller 直接打包：
```bash
pyinstaller linkerhand_service.spec
```

## 服务特性

- **后台运行**：服务以无窗口模式运行，不会弹出控制台窗口
- **日志记录**：所有操作和错误都会记录到日志文件
  - 日志文件保存在 `logs/` 目录下
  - 按日期命名：`linkerhand_service_YYYYMMDD.log`
  - 每个日志文件最大 10MB，自动轮转，保留 5 个备份
- **开机自启动**：可以配置为开机自动启动（见下方说明）

## 使用服务

### 启动服务

**方法一：使用脚本（推荐）**
```bash
start_service.bat
```

**方法二：直接运行**
```bash
linkerhand_service.exe
```

服务将在后台运行，不会显示窗口。

### 停止服务

**方法一：使用脚本（推荐）**
```bash
stop_service.bat
```

**方法二：任务管理器**
在任务管理器中结束 `linkerhand_service.exe` 进程。

**方法三：命令行**
```bash
taskkill /IM linkerhand_service.exe /F
```

### 重启服务

运行：
```bash
restart_service.bat
```

### 卸载服务

运行：
```bash
uninstall_service.bat
```

这将删除开机自启动配置，但不会删除可执行文件和日志文件。

### 查看服务状态

访问健康检查端点：
- 浏览器：`http://localhost:8000/health`
- 命令行：`curl http://localhost:8000/health`

### 查看日志

日志文件保存在服务目录下的 `logs/` 文件夹中，按日期命名。

## 配置开机自启动

### 方法一：使用任务计划程序（推荐）

1. 按 `Win + R`，输入 `taskschd.msc`，回车打开任务计划程序
2. 点击右侧"创建基本任务"
3. 填写任务信息：
   - 名称：`LinkerHand_HTTP_API_Service`
   - 描述：`LinkerHand机械手HTTP API服务 - 开机自启动`
4. 触发器：选择"当用户登录时"
5. 操作：选择"启动程序"
   - 程序或脚本：浏览选择 `linkerhand_service.exe` 的完整路径
   - 起始于：选择 `linkerhand_service.exe` 所在的文件夹
6. 完成设置

### 方法二：使用启动文件夹

1. 按 `Win + R`，输入 `shell:startup`，回车
2. 将 `linkerhand_service.exe` 的快捷方式复制到此文件夹
3. 完成

## 部署到其他电脑

### 一键部署（推荐）

1. **打包服务**
   在开发电脑上运行打包脚本：
   ```bash
   python build.py
   ```
   或双击 `quick_build.bat`

2. **复制 dist 文件夹**
   将整个 `dist` 文件夹复制到目标电脑的任意位置（建议放在 `C:\Program Files\LinkerHand\` 或用户目录下）

3. **一键部署**
   在目标电脑上：
   - 进入 `dist` 文件夹
   - 右键点击 `deploy.bat`
   - 选择"以管理员身份运行"
   - 按照提示完成安装

4. **验证安装**
   - 重启电脑或注销后重新登录
   - 等待服务启动（如果使用任务计划程序，等待30秒）
   - 打开浏览器访问：`http://localhost:8000`
   - 应该能看到 API 信息页面

### 手动部署

如果不想使用一键部署脚本，可以手动配置开机自启动（见下方"配置开机自启动"部分）

## 配置说明

### 修改服务端口

编辑 `Demo/main_http.py` 中的端口设置：

```python
uvicorn.run(
    app,
    host="0.0.0.0",
    port=8000,  # 修改这里
    reload=False,
    log_level="info"
)
```

然后重新打包。

### 修改手部配置

编辑 `Demo/main_http.py` 中的配置：

```python
hand_joint = "O6"      # 手部关节类型
hand_type = "left"     # 左手或右手
modbus = "None"        # Modbus配置
can = "PCAN_USBBUS1"   # CAN通道
```

然后重新打包。

## API 端点

- `GET /` - 获取 API 信息
- `GET /health` - 健康检查
- `GET /docs` - API 文档（Swagger UI）
- `POST /hold_pen` - 执行握笔动作
- `POST /open_hand` - 打开手部
- `POST /close_hand` - 关闭手部
- `POST /set_speed` - 设置速度
- `POST /set_torque` - 设置力矩
- `POST /finger_move` - 移动手指到指定位置

详细 API 文档请访问：`http://localhost:8000/docs`

## 故障排除

### 问题：打包失败

**解决方案：**
1. 确保所有依赖已安装：`pip install -r requirements.txt`
2. 确保 PyInstaller 已安装：`pip install PyInstaller`
3. 检查是否有语法错误

### 问题：服务无法启动

**解决方案：**
1. 检查是否有其他程序占用 8000 端口
2. 检查 CAN 设备是否连接
3. 查看日志文件中的错误信息（`logs/` 目录下）
4. 检查 Windows 防火墙设置

### 问题：开机自启动不工作

**解决方案：**
1. 检查任务计划程序中的任务是否启用
2. 查看任务计划程序中的"历史记录"查看错误信息
3. 检查任务是否设置为"无论用户是否登录都要运行"
4. 尝试使用启动文件夹方式

### 问题：在其他电脑上无法运行

**解决方案：**
1. 确保目标电脑是 Windows 系统
2. 首次运行可能需要点击"更多信息" -> "仍要运行"（Windows 安全警告）
3. 确保目标电脑有必要的系统库（通常 Windows 10/11 已包含）

### 问题：防火墙阻止访问

**解决方案：**
如果需要在其他电脑访问服务，需要在 Windows 防火墙中添加例外规则：

1. 打开"Windows Defender 防火墙"
2. 点击"高级设置"
3. 选择"入站规则" -> "新建规则"
4. 选择"端口" -> "TCP" -> "特定本地端口" -> 输入 `8000`
5. 选择"允许连接"
6. 完成设置

## 文件说明

### 打包相关文件

- `build.py` - 打包脚本
- `quick_build.bat` - 快速打包批处理文件
- `linkerhand_service.spec` - PyInstaller 配置文件
- `runtime_hook.py` - PyInstaller 运行时钩子（修复导入路径）
- `dist_scripts/` - 服务管理脚本目录（打包时自动复制到 dist）

### 打包后的 dist 文件夹

打包完成后，`dist` 文件夹包含：

- `linkerhand_service.exe` - 主程序（必需）
- `deploy.bat` - 一键部署脚本（配置开机自启动）
- `start_service.bat` - 启动服务脚本
- `stop_service.bat` - 停止服务脚本
- `restart_service.bat` - 重启服务脚本
- `uninstall_service.bat` - 卸载服务脚本（删除开机自启动）
- `README.txt` - 使用说明
- `logs/` - 日志目录（服务运行后自动创建）

## 注意事项

1. **首次运行**：首次在目标电脑上运行时，Windows 可能会显示安全警告，需要点击"更多信息" -> "仍要运行"

2. **后台运行**：服务在后台运行，不会显示窗口。如需查看日志，请查看 `logs/` 目录下的日志文件

3. **防火墙**：如果需要在其他电脑访问服务，需要在 Windows 防火墙中添加端口 8000 的例外规则

4. **权限**：某些 CAN 设备可能需要管理员权限才能访问

5. **日志**：服务日志会自动保存到 `logs/` 目录，按日期命名，支持自动轮转

6. **更新**：更新服务时，需要：
   - 重新打包
   - 停止旧服务
   - 替换可执行文件
   - 重启电脑或手动启动新服务

## 技术支持

如有问题，请检查：
1. Python 版本和依赖版本
2. Windows 版本和权限
3. CAN 设备连接状态
4. 防火墙和端口设置
5. 日志文件中的错误信息
